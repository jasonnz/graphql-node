type Query {
	products: [Product]
	productsById(id: ID!): [Product]
	productsByPrice(min: Float!, max: Float!): [Product]
}

type Product {
	id: ID!
	description: String!
	reviews: [Review]
	price: Float!
}

type Review {
	rating: Int!
	comment: String
}
